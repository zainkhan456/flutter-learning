//conversion
void main(){
  Map<String, int> ages = {'Alice': 25, 'Bob': 30, 'Charlie': 22};

  // toMap() → Creates a new map (shallow copy).
  // Map<String, int> agesCopy = ages.toMap();
  // print('Copy: $agesCopy');

// toString() → Returns a string representation.
  print('String: ${ages.toString()}');

  //explaination remaining

}